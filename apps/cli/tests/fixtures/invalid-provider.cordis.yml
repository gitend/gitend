# Invalid override of a builtin provider: an external patch retains the target's required startup policy.

- id: llm-pi-ai
  config:
    providers:
      - provider: openai
        apiKey: keyless-invalid-shape
